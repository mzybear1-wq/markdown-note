# 项目实战 - Markdown 笔记应用 PRD

## 1. 项目概述
本项目旨在开发一个完整的、支持实时预览的 Markdown 笔记应用。用户可以创建笔记、输入标题和 Markdown 格式的内容，并在界面上实时看到渲染后的效果。项目旨在提供流畅的文本编辑体验和专业的代码语法高亮功能。

## 2. 核心功能需求

1. **基础布局与实时预览**
   - 界面采用左右分栏布局：左侧为 Markdown 编辑器（大文本框），右侧为预览区。
   - 在电脑端显示为左右分栏，在手机端自动响应式变成上下布局。
   - 用户在左侧编辑器输入内容时，右侧预览区必须**实时同步**显示 Markdown 渲染后的 HTML 效果。

2. **笔记管理功能**
   - **笔记列表**：左侧边栏（或抽屉）显示所有笔记的标题列表。
   - **笔记切换**：点击列表中的某篇笔记，可将其切换为当前正在编辑/预览的内容。
   - **创建与删除**：支持随时新建空白笔记，以及删除不需要的笔记。
   - **笔记搜索**：支持在笔记列表中按标题进行搜索过滤。

3. **编辑器专业体验增强**
   - **代码高亮**：预览区中的代码块必须支持语法高亮（支持多种编程语言如 JavaScript, Python, Java 等），不同语法元素显示不同颜色。
   - **快捷键支持**：
     - 支持在编辑器内按下 `Tab` 键插入空格，而不是切换浏览器焦点。
     - 支持按下 `Ctrl+B` (或 Cmd+B) 自动为选中文本加粗。
   - **自动保存**：笔记内容需支持自动保存草稿，防止数据丢失。

4. **数据持久化**
   - 所有笔记数据均保存在浏览器的 `LocalStorage` 中。

## 3. 扩展思路 (后续迭代计划)
在基础版完成后，可继续扩展以下功能：
- **图片支持**：支持图片上传，让笔记可以插入本地图片。
- **目录导航**：根据 Markdown 的 Heading 标签自动生成文章大纲 (TOC)。
- **导出分享**：支持将当前笔记导出为 PDF 格式，方便分享。
- **主题切换**：提供多种编辑器主题（如暗黑模式、浅色模式等）。
- **Markdown 风格切换**：支持多种 Markdown 解析风格，比如 GitHub 风格、标准风格等。
